# Placeholder — replace with the kitty.conf from the other machine.
# kitty isn't installed on this machine, so this file was not generated
# from a live config. Once you have the real file, drop it in here
# (this directory is symlinked to ~/.config/kitty/kitty.conf by install.sh).
